---
/**
 * Hero Section
 * Layout: фоновое изображение на весь экран,
 * заголовок слева внизу, описание и кнопки справа внизу
 */
---

<section class="hero">
  <!-- Background Image -->
  <img 
    src="/images/hero/hero-image.png" 
    alt="" 
    class="hero__bg"
    loading="eager"
    fetchpriority="high"
  />

  <!-- Content Container -->
  <div class="hero__content container">
    <!-- Left: Title -->
    <h1 class="hero__title">
      Центр<br />
      регрессивного<br />
      гипноза
    </h1>

    <!-- Right: Description + Buttons -->
    <div class="hero__right">
      <!-- Description Box -->
      <div class="hero__description">
        <p>
          Помогаем адаптировать психические и физические реакции к негативным раздражителям 
          в повседневной жизни, что приводит к внутреннему равновесию и здоровому пофигизму
        </p>
      </div>

      <!-- Buttons -->
      <div class="hero__buttons">
        <!-- Primary Button -->
        <a href="#contact" class="hero__btn hero__btn--primary">
          <div class="hero__btn-icon">
            <img src="/images/icons/icon-arrow.svg" alt="" width="13" height="13" />
          </div>
          <span class="hero__btn-text">
            Записаться<br />на консультацию
          </span>
        </a>

        <!-- Secondary Button -->
        <a href="#training" class="hero__btn hero__btn--secondary">
          <div class="hero__btn-icon">
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M1 12L12 1M12 1H3M12 1V10" stroke="currentColor" stroke-width="2"/>
            </svg>
          </div>
          <span class="hero__btn-text">
            Узнать<br />об обучении
          </span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    width: 100%;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    overflow: hidden;
  }

  .hero__bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    z-index: -1;
  }

  .hero__content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding-bottom: 3rem;
    padding-top: 120px;
    width: 100%;
  }

  @media (min-width: 1024px) {
    .hero__content {
      flex-direction: row;
      align-items: flex-end;
      justify-content: space-between;
      gap: 0;
      padding-bottom: 5rem;
    }
  }

  .hero__title {
    color: white;
    font-family: 'Roboto Flex', sans-serif;
    font-size: clamp(48px, 8vw, 110px);
    font-weight: 700;
    font-variation-settings: 'wdth' 65;
    line-height: 0.86;
    letter-spacing: -0.01em;
    text-transform: uppercase;
    margin: 0;
  }

  .hero__right {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    width: 100%;
  }

  @media (min-width: 1024px) {
    .hero__right {
      max-width: 524px;
      flex-shrink: 0;
    }
  }

  .hero__description {
    /* Liquid Glass Effect */
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.15) 0%,
      rgba(255, 255, 255, 0.05) 50%,
      rgba(255, 255, 255, 0.1) 100%
    );
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 20px;
    padding: 1.25rem 1.5rem;
    box-shadow: 
      0 8px 32px rgba(0, 0, 0, 0.1),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
  }

  .hero__description p {
    color: white;
    font-size: 1rem;
    line-height: 1.4;
    margin: 0;
  }

  @media (min-width: 768px) {
    .hero__description p {
      font-size: 1.25rem;
    }
  }

  .hero__buttons {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.625rem;
    height: auto;
  }

  @media (min-width: 640px) {
    .hero__buttons {
      height: 148px;
    }
  }

  .hero__btn {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 1rem;
    border-radius: 16px;
    text-decoration: none;
    transition: all 0.2s ease;
    min-height: 120px;
  }

  @media (min-width: 640px) {
    .hero__btn {
      min-height: auto;
      height: 100%;
    }
  }

  .hero__btn--primary {
    background-color: #71BB70;
    border-radius: 16px;
  }

  .hero__btn--primary:hover {
    background-color: #5EA85D;
  }

  .hero__btn--secondary {
    background-color: #E5E5E5;
    border-radius: 20px;
  }

  .hero__btn--secondary:hover {
    background-color: #D5D5D5;
  }

  .hero__btn-icon {
    display: flex;
    justify-content: flex-end;
  }

  .hero__btn-icon > * {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
  }

  .hero__btn--primary .hero__btn-icon > * {
    background: rgba(255, 255, 255, 0.2);
  }

  .hero__btn--primary .hero__btn-icon img {
    width: 13px;
    height: 13px;
    filter: none;
  }

  .hero__btn--secondary .hero__btn-icon > * {
    background: rgba(0, 0, 0, 0.1);
    color: #000;
  }

  .hero__btn--secondary .hero__btn-icon svg {
    width: 13px;
    height: 13px;
  }

  .hero__btn-text {
    font-family: 'Roboto Flex', sans-serif;
    font-size: 1.25rem;
    font-weight: 800;
    font-variation-settings: 'wdth' 25;
    line-height: 0.86;
    letter-spacing: -0.01em;
    text-transform: uppercase;
  }

  @media (min-width: 768px) {
    .hero__btn-text {
      font-size: 1.75rem;
    }
  }

  .hero__btn--primary .hero__btn-text {
    color: white;
  }

  .hero__btn--secondary .hero__btn-text {
    color: black;
  }
</style>
